/*!
 * PLACEHOLDER — REPLACE THIS FILE BEFORE LOADING THE EXTENSION
 *
 * The Chrome extension manifest restricts scripts to the extension origin
 * (script-src 'self'), so the Pusher client must ship with the extension
 * rather than load from a CDN.
 *
 * Download the latest Pusher JS browser bundle and save it here:
 *
 *   curl -L https://js.pusher.com/8.4/pusher.min.js -o extension/lib/pusher.min.js
 *
 * Or, from PowerShell:
 *
 *   Invoke-WebRequest -Uri https://js.pusher.com/8.4/pusher.min.js -OutFile extension/lib/pusher.min.js
 *
 * Once replaced, the global `Pusher` constructor will be available inside popup.js.
 */
throw new Error(
  'Popform: pusher.min.js placeholder loaded. ' +
  'Download the real bundle from https://js.pusher.com/8.4/pusher.min.js and overwrite extension/lib/pusher.min.js.'
);
